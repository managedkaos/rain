---
Description: An EC2 instance running Amazon Linux 2023 with Minikube, kubectl, and Docker for Kubernetes exploration; includes a security group allowing TCP traffic on port 80, and an IAM role with permissions for Systems Manager.

Parameters:
  InstanceType:
    Description: The size to use for the EC2 instance
    Type: String
    AllowedValues:
      - t3.medium
      - t3.large
      - t3.xlarge
      - t3.2xlarge
    Default: t3.large
    ConstraintDescription: Must be a valid EC2 instance type. Minikube requires at least 2 CPUs and 2GB RAM.

Resources:
  Role:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com
            Action:
              - sts:AssumeRole
      Path: /
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonEC2RoleforSSM

  Profile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Path: /
      Roles:
        - !Ref Role

  SecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow TCP traffic on port 80
      SecurityGroupIngress:
        - CidrIp: 0.0.0.0/0
          IpProtocol: tcp
          FromPort: 80
          ToPort: 80

  Server:
    CreationPolicy:
      ResourceSignal:
        Timeout: PT15M
    Type: AWS::EC2::Instance
    Metadata:
      AWS::CloudFormation::Init:
        configSets:
          application_installation:
            - install_cfn
            - install_apps
            - install_docker
            - install_kubectl
            - install_crictl
            - install_minikube
        install_cfn:
          files:
            /etc/cfn/cfn-hup.conf:
              content: !Sub |
                [main]
                stack=${AWS::StackId}
                region=${AWS::Region}
              mode: "000400"
              owner: root
              group: root
            /etc/cfn/hooks.d/cfn-auto-reloader.conf:
              content: !Sub |
                [cfn-auto-reloader-hook]
                triggers=post.update
                path=Resources.Server.Metadata.AWS::CloudFormation::Init
                action=/opt/aws/bin/cfn-init -v
                      --stack ${AWS::StackName}
                      --resource Server
                      --configsets application_installation
                      --region ${AWS::Region}
              mode: "000400"
              owner: root
              group: root
          services:
            systemd:
              cfn-hup:
                enabled: "true"
                ensureRunning: "true"
                files:
                  - /etc/cfn/cfn-hup.conf
                  - /etc/cfn/hooks.d/cfn-auto-reloader.conf
        install_apps:
          packages:
            yum:
              git: []
              conntrack-tools: []
        install_docker:
          commands:
            01_install_docker:
              command: dnf install -y docker
            02_start_docker:
              command: systemctl enable --now docker
            03_add_ec2_user_to_docker:
              command: usermod -aG docker ec2-user
            04_add_ssm_user_to_docker:
              command: usermod -aG docker ssm-user
        install_kubectl:
          commands:
            01_download_kubectl:
              command: curl -LO "https://dl.k8s.io/release/$(curl -Ls https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
              cwd: /tmp
            02_install_kubectl:
              command: install -o root -g root -m 0755 /tmp/kubectl /usr/local/bin/kubectl
        install_crictl:
          commands:
            01_download_crictl:
              command: curl -L https://github.com/kubernetes-sigs/cri-tools/releases/download/v1.31.1/crictl-v1.31.1-linux-amd64.tar.gz -o /tmp/crictl.tar.gz
            02_install_crictl:
              command: tar -C /usr/local/bin -xzf /tmp/crictl.tar.gz && chmod +x /usr/local/bin/crictl
        install_minikube:
          commands:
            01_download_minikube:
              command: curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
              cwd: /tmp
            02_install_minikube:
              command: install -o root -g root -m 0755 /tmp/minikube-linux-amd64 /usr/local/bin/minikube
    Properties:
      InstanceType: !Ref InstanceType
      ImageId: '{{resolve:ssm:/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-6.1-x86_64}}'
      BlockDeviceMappings:
        - DeviceName: /dev/xvda
          Ebs:
            VolumeSize: 50
            VolumeType: gp3
            DeleteOnTermination: true
      IamInstanceProfile: !Ref Profile
      SecurityGroups:
        - !Ref SecurityGroup
      Tags:
        - Key: Name
          Value: !Sub ${AWS::StackName}
      UserData: !Base64
        Fn::Sub: |
          #!/bin/bash -xe
          exec > >(tee /var/log/user-data.log) 2>&1

          dnf update -y aws-cfn-bootstrap

          /opt/aws/bin/cfn-init -v \
            --stack ${AWS::StackName} \
            --region ${AWS::Region} \
            --resource Server \
            --configsets application_installation

          /opt/aws/bin/cfn-signal -e $? \
            --stack ${AWS::StackName} \
            --region ${AWS::Region} \
            --resource Server

  ElasticIP:
    Type: AWS::EC2::EIP
    Properties:
      InstanceId: !Ref Server

Outputs:
  TerminalURL:
    Description: Follow this URL to open a terminal on the server
    Value: !Sub https://${AWS::Region}.console.aws.amazon.com/systems-manager/session-manager/${Server}?region=${AWS::Region}
  MinikubeStartCommand:
    Description: Run this command after connecting to start Minikube
    Value: minikube start --driver=docker
  PublicIP:
    Description: The public IP address of the instance
    Value: !Ref ElasticIP
